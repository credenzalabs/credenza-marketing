# Homepage archive — 2026-07-23

Snapshot of the Credenza homepage taken before the hero / "How it works" /
resale-certificate-management copy updates.

- `Home.tsx` — copy of `src/pages/Home.tsx` (section ordering).
- `sections/` — copy of every file in `src/components/sections/home/` at this date.

This is a reference archive only; it is not imported or built. To restore a
section, copy the file back over its counterpart in `src/`.
